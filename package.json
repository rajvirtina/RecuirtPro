{
  "name": "recruit-pro",
  "version": "1.0.0",
  "description": "Recruitment Process Automation Platform",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install --omit=dev && npm run build",
    "start": "cd backend && node dist/server.js",
    "dev": "cd backend && npm run dev",
    "build:full": "cd backend && npm install && npm run build && cd ../frontend && npm install && npm run build",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
